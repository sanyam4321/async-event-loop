#pragma once


/*Need to handle socket errors and disconnections as well and then clean up*/
namespace FiberConn{
    class HttpConnection{
        private:
            int socket;
        public:
            HttpConnection(){

            }
    };
}